#ifndef STR_HDR_H
#define STR_HDR_H
#include <stdio.h>

/* Gets the length of a string */
int _strlen(char *str);

/* Compares 2 strings */
int _strcmp(char *s1, char *s2);

/* Compares 2 string up to n chars */
int _strncmp(char *s1, char *s2, int n);

#endif
